@model mvcLearn.ViewModels.HomeIndexViewModel

<div class="text-center">
    <h1>@Model.CurrentMessage</h1>  
    <table>
        @foreach (var restaurant in Model.Restaurent)
        {
            <tr>
                <td>@restaurant.Id</td>
                <td>@restaurant.Name</td>
            </tr>
            <tr></tr>
        }
    </table>       
</div>

